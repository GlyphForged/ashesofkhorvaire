# LAN-only virtual host for the Ashes of Khorvaire wiki.
# Pi-hole resolves this hostname to the Orange Pi; Lighttpd proxies only this host.
$HTTP["host"] == "aok.glyphforged.com" {
  proxy.server = (
    "" => ((
      "host" => "127.0.0.1",
      "port" => 3000
    ))
  )
}
