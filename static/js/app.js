document.addEventListener("keydown",(event)=>{if(event.key==="/"&&!/INPUT|TEXTAREA|SELECT/.test(document.activeElement.tagName)){event.preventDefault();document.querySelector("#nav-search")?.focus();}});
document.querySelectorAll("time[data-unix]").forEach((el)=>{const date=new Date(Number(el.dataset.unix)*1000);if(!Number.isNaN(date.valueOf())){el.dateTime=date.toISOString();el.textContent=new Intl.DateTimeFormat(undefined,{dateStyle:"medium",timeStyle:"short"}).format(date);}});
document.body.addEventListener("htmx:afterSwap",()=>document.querySelectorAll("time[data-unix]").forEach((el)=>{const date=new Date(Number(el.dataset.unix)*1000);el.textContent=new Intl.DateTimeFormat(undefined,{dateStyle:"medium",timeStyle:"short"}).format(date);}));
if(new URLSearchParams(location.search).has("saved")){const status=document.createElement("p");status.className="notice save-status";status.setAttribute("role","status");status.textContent="Dossier saved.";document.querySelector(".page-header")?.after(status);history.replaceState({},"",location.pathname);}
